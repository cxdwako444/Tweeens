--!nocheck
-- ============================================================
-- MACHINA RECON v13  —  HONEST METRICS + FOCUSED FIRE
-- Branch: claude/rare-indicator-auto-aim-mC8LF
-- File:   MachinaRecon.txt
--
-- WHAT v12 LIED ABOUT:
--   mined/s counted ALL part removals from workspace.Blocks. But
--   Roblox streams those parts in/out as you move — most removals
--   are streaming, NOT mining. That's why you saw spikes of 575/s
--   from only 54 fires (physically impossible).
--
-- v13 CHANGES:
--   1. mined/s only counts removals within MINE_PROXIMITY studs
--      (default 30) of your character. Streaming events at the map
--      edges no longer inflate the number.
--   2. NEW "MaxTgts" pill — caps how many of the closest blocks we
--      fire at per cycle. Spreading 60 fires across 140 blocks gives
--      each one too little to break. Concentrate fires → blocks break.
--   3. Status now shows "mines/fire" efficiency ratio.
--   4. The damage value is clamped server-side regardless of what
--      you send. dmg=1 is fine; dmg=100 doesn't help. Default 1.
-- ============================================================

local Players          = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage= game:GetService("ReplicatedStorage")
local Workspace        = game:GetService("Workspace")
local LocalPlayer = Players.LocalPlayer
local PlayerGui   = LocalPlayer:WaitForChild("PlayerGui")

local OUT_FILE        = "MachinaRecon.txt"
local MAGIC_NONCE     = string.char(255,255,255,255)
local R               = ReplicatedStorage:WaitForChild("RemoteEvent")
local ORIGIN          = Vector3.new(12000, 12000, 12000)
local BLOCK_SIZE      = 6
local MINE_PROXIMITY  = 30  -- studs: only count nearby removals as mines

-- ============================================================
-- ENV
-- ============================================================
local function find(n)
	local v = rawget(_G, n); if v then return v end
	local ok, env = pcall(getfenv, 0); if ok and env then v = env[n]; if v then return v end end
	local ok2, gg = pcall(getgenv); if ok2 and gg then v = gg[n]; if v then return v end end
	return nil
end
local _writefile = find("writefile")
local _readfile  = find("readfile")
local _isfile    = find("isfile")

-- ============================================================
-- LOG
-- ============================================================
local LOG = {}
local LOG_MAX = 400
local function tstamp() return os.date("%H:%M:%S") end
local function log(msg)
	table.insert(LOG, ("[%s] %s"):format(tstamp(), tostring(msg)))
	if #LOG > LOG_MAX then table.remove(LOG, 1) end
end

-- ============================================================
-- BUFFER + FIRE
-- ============================================================
local function snapCoord(c, origin)
	return math.floor((c - origin) / BLOCK_SIZE + 0.5) * BLOCK_SIZE + origin
end
local function makeMineBuffer(wx, wy, wz, damage)
	local b = buffer.create(32)
	buffer.writef64(b, 0,  (snapCoord(wz, ORIGIN.Z) - ORIGIN.Z) / BLOCK_SIZE)
	buffer.writef64(b, 8,  (snapCoord(wy, ORIGIN.Y) - ORIGIN.Y) / BLOCK_SIZE)
	buffer.writef64(b, 16, (snapCoord(wx, ORIGIN.X) - ORIGIN.X) / BLOCK_SIZE)
	buffer.writef64(b, 24, damage)
	return b
end
local function randomNonce()
	return string.char(
		math.random(0,255), math.random(0,255),
		math.random(0,255), math.random(0,255))
end
local function fireMine(x, y, z, dmg)
	pcall(function()
		R:FireServer(randomNonce(), "DamageBlockSmooth",
			makeMineBuffer(x, y, z, dmg))
	end)
end

-- ============================================================
-- TARGETS
-- ============================================================
local function collectTargets(playerPos, radius, mode)
	local r2 = radius * radius
	local seen = {}
	local result = {}
	local function tryAdd(part)
		if not part:IsA("BasePart") then return end
		local p = part.Position
		local dp = p - playerPos
		local d2 = dp.X*dp.X + dp.Y*dp.Y + dp.Z*dp.Z
		if mode == "Near" and d2 > r2 then return end
		local kx = math.floor((p.X - ORIGIN.X) / BLOCK_SIZE + 0.5)
		local ky = math.floor((p.Y - ORIGIN.Y) / BLOCK_SIZE + 0.5)
		local kz = math.floor((p.Z - ORIGIN.Z) / BLOCK_SIZE + 0.5)
		local key = kx .. "," .. ky .. "," .. kz
		if seen[key] then return end
		seen[key] = true
		table.insert(result, {x=p.X, y=p.Y, z=p.Z, d2=d2})
	end
	local cp = Workspace:FindFirstChild("CollisionParts")
	if cp then for _, d in ipairs(cp:GetChildren()) do tryAdd(d) end end
	local bl = Workspace:FindFirstChild("Blocks")
	if bl then
		for _, child in ipairs(bl:GetChildren()) do
			if child:IsA("BasePart") then tryAdd(child)
			elseif child:IsA("Folder") or child:IsA("Model") then
				for _, gc in ipairs(child:GetChildren()) do tryAdd(gc) end
			end
		end
	end
	table.sort(result, function(a, b) return a.d2 < b.d2 end)
	return result
end

-- ============================================================
-- STATE
-- ============================================================
local stats = {
	radiusOn = false,
	mode = "Near",
	radius = 18,
	rateHz = 10,
	damage = 1.0,
	cap = 60,
	maxTgts = 10,         -- only fire at the N closest of the targets list
	totalFires = 0,
	firesThisSec = 0,
	firesPerSec = 0,
	minedThisSec = 0,
	minedPerSec = 0,
	totalMined = 0,
	targetsFound = 0,
	cyclesRan = 0,
}

-- ============================================================
-- HONEST MINE COUNTER  (only nearby removals)
-- ============================================================
local function attachWatcher(folder)
	if not folder then return end
	folder.DescendantRemoving:Connect(function(c)
		if not c:IsA("BasePart") then return end
		local char = LocalPlayer.Character
		local hrp = char and char:FindFirstChild("HumanoidRootPart")
		if not hrp then return end
		local dp = c.Position - hrp.Position
		local d2 = dp.X*dp.X + dp.Y*dp.Y + dp.Z*dp.Z
		if d2 <= MINE_PROXIMITY * MINE_PROXIMITY then
			stats.minedThisSec = stats.minedThisSec + 1
			stats.totalMined = stats.totalMined + 1
		end
	end)
end
attachWatcher(Workspace:FindFirstChild("CollisionParts"))
attachWatcher(Workspace:FindFirstChild("Blocks"))
Workspace.ChildAdded:Connect(function(c)
	if c.Name == "CollisionParts" or c.Name == "Blocks" then attachWatcher(c) end
end)

-- per-second tick + log
task.spawn(function()
	while true do
		task.wait(1.0)
		stats.minedPerSec = stats.minedThisSec; stats.minedThisSec = 0
		stats.firesPerSec = stats.firesThisSec; stats.firesThisSec = 0
		if stats.radiusOn then
			local mpf = (stats.firesPerSec > 0)
				and (stats.minedPerSec / stats.firesPerSec) or 0
			log(("tick: tgts=%d fires/s=%d mined/s=%d (%.1f%%) total=%d"):format(
				stats.targetsFound, stats.firesPerSec, stats.minedPerSec,
				mpf * 100, stats.totalMined))
		end
	end
end)

-- ============================================================
-- LOOP — fire only top maxTgts closest blocks
-- ============================================================
task.spawn(function()
	while true do
		if stats.radiusOn then
			local char = LocalPlayer.Character
			local hrp = char and char:FindFirstChild("HumanoidRootPart")
			if hrp then
				local targets = collectTargets(hrp.Position, stats.radius, stats.mode)
				stats.targetsFound = #targets
				-- Pick the closest N (focused fire)
				local nTargets = math.min(stats.maxTgts, #targets)
				-- Rate cap across the chosen targets
				local maxFiresThisCycle = math.max(1, math.floor(stats.cap / math.max(1, stats.rateHz)))
				local fireQty = math.min(maxFiresThisCycle, nTargets)
				for i = 1, fireQty do
					local t = targets[i]
					fireMine(t.x, t.y, t.z, stats.damage)
					stats.totalFires = stats.totalFires + 1
					stats.firesThisSec = stats.firesThisSec + 1
				end
				stats.cyclesRan = stats.cyclesRan + 1
			else
				stats.targetsFound = 0
			end
			task.wait(1 / math.max(1, stats.rateHz))
		else
			task.wait(0.1)
		end
	end
end)

-- ============================================================
-- UI (same shape as v12 + MaxTgts pill)
-- ============================================================
local C = {
	BG=Color3.fromRGB(14,19,33), DEEP=Color3.fromRGB(5,8,16),
	CARD=Color3.fromRGB(22,30,50), TXT=Color3.fromRGB(240,245,255),
	DIM=Color3.fromRGB(135,155,185), PINK=Color3.fromRGB(245,90,165),
	OK=Color3.fromRGB(80,195,130), WARN=Color3.fromRGB(235,175,75),
	BLUE=Color3.fromRGB(80,150,235), RED=Color3.fromRGB(225,85,95),
	GOLD=Color3.fromRGB(255,200,80),
}
local function corner(o,r) local c=Instance.new("UICorner") c.CornerRadius=UDim.new(0,r or 6) c.Parent=o end
local function stroke(o,col,t) local s=Instance.new("UIStroke") s.Color=col s.Thickness=t or 1 s.Parent=o end

local old = PlayerGui:FindFirstChild("MachinaReconV13")
if old then old:Destroy() end

local gui = Instance.new("ScreenGui")
gui.Name = "MachinaReconV13"
gui.ResetOnSpawn = false
gui.IgnoreGuiInset = true
gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
gui.DisplayOrder = 9999
gui.Parent = PlayerGui

local root = Instance.new("Frame")
root.Size = UDim2.new(0, 290, 0, 560)
root.Position = UDim2.new(0, 12, 0, 60)
root.BackgroundColor3 = C.BG
root.BorderSizePixel = 0
root.Active = true
root.ZIndex = 10000
root.Parent = gui
corner(root, 8); stroke(root, C.DEEP, 1)

local function setZIndex(p, z)
	if p:IsA("GuiObject") then p.ZIndex = z end
	for _, c in ipairs(p:GetChildren()) do setZIndex(c, z) end
end

-- Title
local titleBar = Instance.new("Frame")
titleBar.Size=UDim2.new(1,0,0,24); titleBar.BackgroundColor3=C.DEEP
titleBar.BorderSizePixel=0; titleBar.Parent=root; corner(titleBar,8)
local fix=Instance.new("Frame"); fix.Size=UDim2.new(1,0,0,10)
fix.Position=UDim2.new(0,0,1,-10); fix.BackgroundColor3=C.DEEP
fix.BorderSizePixel=0; fix.Parent=titleBar
local titleLbl=Instance.new("TextLabel"); titleLbl.Size=UDim2.new(1,-50,1,0)
titleLbl.Position=UDim2.new(0,8,0,0); titleLbl.BackgroundTransparency=1
titleLbl.Text="RECON v13 — honest metrics"; titleLbl.Font=Enum.Font.GothamBlack
titleLbl.TextSize=10; titleLbl.TextColor3=C.GOLD
titleLbl.TextXAlignment=Enum.TextXAlignment.Left; titleLbl.Parent=titleBar

local hideBtn = Instance.new("TextButton")
hideBtn.Size=UDim2.new(0,22,0,20); hideBtn.Position=UDim2.new(1,-26,0,2)
hideBtn.BackgroundColor3=C.CARD; hideBtn.AutoButtonColor=false
hideBtn.Text="−"; hideBtn.Font=Enum.Font.GothamBlack
hideBtn.TextSize=14; hideBtn.TextColor3=C.TXT
hideBtn.Parent=titleBar; corner(hideBtn,4)

local status=Instance.new("TextLabel")
status.Size=UDim2.new(1,-12,0,76); status.Position=UDim2.new(0,6,0,28)
status.BackgroundColor3=C.CARD; status.BorderSizePixel=0
status.Font=Enum.Font.Code; status.TextSize=10; status.TextColor3=C.OK
status.Text="  ready"; status.TextXAlignment=Enum.TextXAlignment.Left
status.TextYAlignment=Enum.TextYAlignment.Top
status.TextWrapped=true; status.Parent=root; corner(status,5)
local sp=Instance.new("UIPadding"); sp.PaddingLeft=UDim.new(0,6); sp.PaddingTop=UDim.new(0,4); sp.Parent=status

local function btnPair(y, lT, lC, lF, rT, rC, rF)
	local lb = Instance.new("TextButton")
	lb.Size=UDim2.new(0.5,-9,0,26); lb.Position=UDim2.new(0,6,0,y)
	lb.BackgroundColor3=lC; lb.AutoButtonColor=false
	lb.Text=lT; lb.Font=Enum.Font.GothamBold; lb.TextSize=10
	lb.TextColor3=C.TXT; lb.Parent=root; corner(lb,5)
	lb.MouseButton1Click:Connect(lF)
	local rb = Instance.new("TextButton")
	rb.Size=UDim2.new(0.5,-9,0,26); rb.Position=UDim2.new(0.5,3,0,y)
	rb.BackgroundColor3=rC; rb.AutoButtonColor=false
	rb.Text=rT; rb.Font=Enum.Font.GothamBold; rb.TextSize=10
	rb.TextColor3=C.TXT; rb.Parent=root; corner(rb,5)
	rb.MouseButton1Click:Connect(rF)
	return lb, rb
end
local function btnFull(y, text, color, click)
	local b = Instance.new("TextButton")
	b.Size=UDim2.new(1,-12,0,26); b.Position=UDim2.new(0,6,0,y)
	b.BackgroundColor3=color; b.AutoButtonColor=false
	b.Text=text; b.Font=Enum.Font.GothamBold; b.TextSize=10
	b.TextColor3=C.TXT; b.Parent=root; corner(b,5)
	b.MouseButton1Click:Connect(click)
	return b
end
local function pillRow(y, label, values, getter, setter, fmt)
	fmt = fmt or tostring
	local lbl = Instance.new("TextLabel")
	lbl.Size=UDim2.new(0,55,0,20); lbl.Position=UDim2.new(0,6,0,y)
	lbl.BackgroundTransparency=1; lbl.Text=label
	lbl.Font=Enum.Font.GothamBold; lbl.TextSize=9; lbl.TextColor3=C.DIM
	lbl.TextXAlignment=Enum.TextXAlignment.Left; lbl.Parent=root
	local boxes = {}
	local total = 290 - 70
	for i, v in ipairs(values) do
		local pillW = (total / #values) - 4
		local b = Instance.new("TextButton")
		b.Size=UDim2.new(0,pillW,0,20)
		b.Position=UDim2.new(0, 65+(i-1)*(pillW+4), 0, y)
		b.BackgroundColor3 = (getter() == v) and C.BLUE or C.CARD
		b.AutoButtonColor=false; b.Text=fmt(v); b.Font=Enum.Font.GothamBold
		b.TextSize=9; b.TextColor3=C.TXT; b.Parent=root; corner(b,4)
		b.MouseButton1Click:Connect(function()
			setter(v)
			for j, bb in ipairs(boxes) do
				bb.BackgroundColor3 = (values[j] == v) and C.BLUE or C.CARD
			end
		end)
		boxes[i] = b
	end
end

btnPair(110,
	"★ AUTO ON", C.OK, function()
		pcall(function() R:FireServer(MAGIC_NONCE, "SetAutoMine", true) end)
		log("→ SetAutoMine TRUE")
	end,
	"■ AUTO OFF", C.RED, function()
		pcall(function() R:FireServer(MAGIC_NONCE, "SetAutoMine", false) end)
		log("→ SetAutoMine FALSE")
	end)

local rmBtn
rmBtn = btnFull(140, "RADIUS MINE: OFF", C.RED, function()
	stats.radiusOn = not stats.radiusOn
	rmBtn.Text = stats.radiusOn and "RADIUS MINE: ON ✦" or "RADIUS MINE: OFF"
	rmBtn.BackgroundColor3 = stats.radiusOn and C.OK or C.RED
	log(("RM %s mode=%s r=%d hz=%d cap=%d tgt=%d dmg=%s"):format(
		stats.radiusOn and "ON" or "OFF", stats.mode, stats.radius,
		stats.rateHz, stats.cap, stats.maxTgts, tostring(stats.damage)))
end)

pillRow(172, "Mode",    {"Near","All"},      function() return stats.mode end,    function(v) stats.mode=v end)
pillRow(198, "Radius",  {8,12,18,24,36},     function() return stats.radius end,  function(v) stats.radius=v end)
pillRow(224, "MaxTgts", {1,5,10,20,50},      function() return stats.maxTgts end, function(v) stats.maxTgts=v end)
pillRow(250, "Rate Hz", {5,10,20,30},        function() return stats.rateHz end,  function(v) stats.rateHz=v end)
pillRow(276, "Cap/s",   {30,60,100,200},     function() return stats.cap end,     function(v) stats.cap=v end)
pillRow(302, "Damage",  {0.05,0.5,1,5},      function() return stats.damage end,  function(v) stats.damage=v end)

local saveBtn
saveBtn = btnFull(336, "💾 SAVE LOG TO FILE", C.BLUE, function()
	if not _writefile then saveBtn.Text="✗ NO WRITEFILE"; return end
	local hdr = {
		"===== MACHINA RECON v13 LOG =====",
		("Saved: %s"):format(os.date("%Y-%m-%d %H:%M:%S")),
		("Player: %s  PlaceId: %d"):format(LocalPlayer.Name, game.PlaceId),
		("RM=%s mode=%s r=%d hz=%d cap=%d maxTgts=%d dmg=%s"):format(
			stats.radiusOn and "ON" or "OFF", stats.mode, stats.radius,
			stats.rateHz, stats.cap, stats.maxTgts, tostring(stats.damage)),
		("fires_total=%d  mined_total=%d  fires/s=%d  mined/s=%d"):format(
			stats.totalFires, stats.totalMined, stats.firesPerSec, stats.minedPerSec),
		("targets_now=%d  cycles=%d  mine_proximity=%d studs"):format(
			stats.targetsFound, stats.cyclesRan, MINE_PROXIMITY),
		"",
		"===== EVENT LOG (last "..#LOG..") =====",
	}
	local body = {}
	for i, line in ipairs(LOG) do body[i] = line end
	local payload = table.concat(hdr, "\n") .. "\n" .. table.concat(body, "\n")
	local ok, err = pcall(_writefile, OUT_FILE, payload)
	if ok then
		saveBtn.Text = ("✓ %d bytes"):format(#payload)
		log(("✓ saved log: %d bytes"):format(#payload))
	else
		saveBtn.Text = "✗ FAIL"
		log("✗ writefile err: " .. tostring(err))
	end
	task.delay(2.5, function() if saveBtn.Parent then saveBtn.Text = "💾 SAVE LOG TO FILE" end end)
end)

btnPair(368,
	"+ Mark", C.WARN, function() log("=== MARK ===") end,
	"🗑 Clear", C.CARD, function() LOG = {}; log("log cleared") end)

-- LOG PANEL
local logTitle = Instance.new("TextLabel")
logTitle.Size=UDim2.new(1,-12,0,14); logTitle.Position=UDim2.new(0,6,0,400)
logTitle.BackgroundTransparency=1; logTitle.Text="LIVE LOG"
logTitle.Font=Enum.Font.GothamBold; logTitle.TextSize=9
logTitle.TextColor3=C.DIM; logTitle.TextXAlignment=Enum.TextXAlignment.Left
logTitle.Parent=root

local logFrame = Instance.new("ScrollingFrame")
logFrame.Size=UDim2.new(1,-12,1,-422); logFrame.Position=UDim2.new(0,6,0,418)
logFrame.BackgroundColor3=C.DEEP; logFrame.BorderSizePixel=0
logFrame.ScrollBarThickness=4; logFrame.AutomaticCanvasSize=Enum.AutomaticSize.Y
logFrame.CanvasSize=UDim2.new(0,0,0,0)
logFrame.ScrollBarImageColor3=C.DIM; logFrame.Parent=root; corner(logFrame,5)

local logText = Instance.new("TextLabel")
logText.Size=UDim2.new(1,-8,0,0); logText.Position=UDim2.new(0,4,0,4)
logText.AutomaticSize=Enum.AutomaticSize.Y; logText.BackgroundTransparency=1
logText.Font=Enum.Font.Code; logText.TextSize=8; logText.TextColor3=C.TXT
logText.TextXAlignment=Enum.TextXAlignment.Left
logText.TextYAlignment=Enum.TextYAlignment.Top
logText.TextWrapped=true; logText.Text=""; logText.Parent=logFrame

setZIndex(root, 10000)

do
	local dragging, ds, sp_ = false, nil, nil
	titleBar.InputBegan:Connect(function(input)
		if input.UserInputType==Enum.UserInputType.MouseButton1
			or input.UserInputType==Enum.UserInputType.Touch then
			dragging=true; ds=input.Position; sp_=root.Position
			input.Changed:Connect(function()
				if input.UserInputState==Enum.UserInputState.End then dragging=false end
			end)
		end
	end)
	UserInputService.InputChanged:Connect(function(input)
		if not dragging then return end
		if input.UserInputType~=Enum.UserInputType.MouseMovement
			and input.UserInputType~=Enum.UserInputType.Touch then return end
		local d = input.Position - ds
		root.Position = UDim2.new(sp_.X.Scale, sp_.X.Offset+d.X,
			sp_.Y.Scale, sp_.Y.Offset+d.Y)
	end)
end

local bubble = Instance.new("TextButton")
bubble.Size=UDim2.new(0,44,0,44); bubble.Position=UDim2.new(0,12,0,12)
bubble.BackgroundColor3=C.GOLD; bubble.AutoButtonColor=false
bubble.Text="R13"; bubble.Font=Enum.Font.GothamBlack
bubble.TextSize=12; bubble.TextColor3=C.DEEP
bubble.Visible=false; bubble.ZIndex=10100; bubble.Parent=gui
do
	local c=Instance.new("UICorner") c.CornerRadius=UDim.new(1,0) c.Parent=bubble
	local s=Instance.new("UIStroke") s.Color=C.TXT s.Thickness=2 s.Parent=bubble
end
hideBtn.MouseButton1Click:Connect(function() root.Visible=false; bubble.Visible=true end)
bubble.MouseButton1Click:Connect(function() root.Visible=true; bubble.Visible=false end)

task.spawn(function()
	while gui.Parent do
		task.wait(0.3)
		if status.Parent then
			local eff = (stats.firesPerSec > 0)
				and (stats.minedPerSec / stats.firesPerSec * 100) or 0
			status.Text = ("  STATE: %s  mode=%s  maxTgts=%d\n  tgts: %d   fires/s: %d (cap %d)\n  MINED/s: %d (%.0f%% efficiency)\n  TOTAL: %d   mine_prox=%dst"):format(
				stats.radiusOn and "ON ✦" or "off", stats.mode, stats.maxTgts,
				stats.targetsFound, stats.firesPerSec, stats.cap,
				stats.minedPerSec, eff, stats.totalMined, MINE_PROXIMITY)
		end
		if logText.Parent then
			local start = math.max(1, #LOG - 35)
			local arr = {}
			for i = start, #LOG do arr[#arr+1] = LOG[i] end
			logText.Text = table.concat(arr, "\n")
			logFrame.CanvasPosition = Vector2.new(0, logText.AbsoluteSize.Y)
		end
	end
end)

log("v13 ready · honest mine counter (within 30 studs only)")
log("try maxTgts=5 first for focused fire")
